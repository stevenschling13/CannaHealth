# Public base URL for the backend API used by the frontend at runtime.
NEXT_PUBLIC_API_BASE_URL=http://localhost:8000

# Optional: enable Vercel analytics (leave blank to disable in non-production environments).
NEXT_PUBLIC_VERCEL_ANALYTICS_ID=
